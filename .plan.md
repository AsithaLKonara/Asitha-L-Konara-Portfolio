# Enhancement Plan

## Admin Analytics & UX
- Add analytics summary to `app/admin/page.tsx` (weekly counts, recent contacts).
- Implement filters/search in admin lists (client components).
- Integrate WYSIWYG editor (e.g., TipTap) for article content editing.
- Add image upload support (use next/image, storage TBD).
